# Godot 4+ specific ignores
.godot/
.nomedia

# Godot-specific ignores
.import/
export.cfg
export_credentials.cfg

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# Résidus d'édition
*.tmp
desktop.ini
Thumbs.db
# Référence locale du test de non-régression du relief (voir heightmap_signature.gd).
# Non versionnée : elle se réécrit à chaque changement légitime de carte, et une
# référence qu'on met à jour par réflexe ne teste plus rien.
world/terrain/heightmap_signature.txt
